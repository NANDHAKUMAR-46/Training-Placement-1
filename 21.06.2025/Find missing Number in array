public class MissingNumber {
    public static void main(String[] args) {
        int[] arr = {1, 2, 4, 5, 6};
        int n = 6, total = n * (n + 1) / 2;
        for (int num : arr)
            total -= num;
        System.out.println("Missing Number: " + total);
    }
}
