public class DigitCountRec {
    static int countDigits(int num) {
        if (num == 0) return 0;
        return 1 + countDigits(num / 10);
    }

    public static void main(String[] args) {
        System.out.println("Digits: " + countDigits(12345));
    }
}
